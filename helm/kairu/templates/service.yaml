apiVersion: v1
kind: Service
metadata:
  name: {{ include "kairu.fullname" . }}
  labels:
    {{- include "kairu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kairu.selectorLabels" . | nindent 4 }}
